# Native webserv implementation

The implementation will use EuryOS-native network and filesystem session
capabilities. Keep host-only previews and protocol tests separate from the
EL0 target implementation; a host test is not evidence that the package can
run on EuryOS.
